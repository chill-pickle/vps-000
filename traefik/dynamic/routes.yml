http:
  routers:
    outline:
      rule: "Host(`outline.chillpickle.org`)"
      entryPoints: [websecure]
      service: outline

    chillang-api:
      rule: "Host(`api.chillang.chillpickle.org`)"
      entryPoints: [websecure]
      service: chillang-api

  services:
    outline:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8089"

    chillang-api:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8091"
