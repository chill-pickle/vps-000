[project]
name = "story-api"
version = "0.1.0"
description = "Story tracker API with FastAPI + SQLite"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "sqlmodel>=0.0.21",
]
